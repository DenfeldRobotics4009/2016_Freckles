import math

class Settings():
    """Some settings stuff that may or may not help out with our attempts to change 'sensitivity' settings."""

    num_precision = 0.5
    num_scaling = 0.75
    num_drive = 4
